{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "greatsuccess",
         "namespace": "com-att-ocnp-mgmt"
    },
    "spec": {
        "selector": {
            "app": "greatsuccess"
        },
        "ports": [
            {
                "protocol": "TCP",
                "port": 80,
                "targetPort": 8080
            }
        ],
        "type": "NodePort"
    }
}
